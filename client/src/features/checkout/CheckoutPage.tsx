import { Typography } from "@mui/material";

export default function CheckoutPage() {
    return (
        <Typography variant='h3'>
           Sadece giriş yapmış kullanıcılar bu sayfayı görüntüleyebilir
        </Typography>
    )
}